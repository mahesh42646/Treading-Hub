{
  "name": "trading-hub-webhook",
  "version": "1.0.0",
  "description": "GitHub webhook for automated deployment",
  "main": "deploy-webhook.js",
  "scripts": {
    "start": "node deploy-webhook.js",
    "dev": "nodemon deploy-webhook.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
